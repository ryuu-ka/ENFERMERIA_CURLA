{{#each faq}}
<div class="card">
    <div class="card-header">
        <strong>{{pp}}</strong>
    </div>
    <div class="card-body">
        <blockquote class="blockquote mb-0">
            <p>{{rr}}</p>
        </blockquote>
    </div>
</div>
{{/each}}